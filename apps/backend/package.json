{
  "name": "@daevox/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --experimental-ffi --watch src/main.ts",
    "start": "node --experimental-ffi src/main.ts",
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json",
    "verify": "npm run typecheck && npm test"
  },
  "dependencies": {
    "@daevox/framework": "0.0.1",
    "@daevox/tdlib": "0.0.0",
    "@daevox/telegram-contract": "0.0.0",
    "dotenv": "^17.4.2"
  },
  "engines": {
    "node": ">= 26",
    "npm": ">= 12"
  }
}
