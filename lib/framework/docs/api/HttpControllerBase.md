<!-- Generated by documentation.js. Update this documentation by updating the source code. -->

# HttpControllerBase

Source / Исходный модуль: [`src/HttpControllerBase.ts`](../../src/HttpControllerBase.ts)

## HttpControllerOptions

Dependencies supplied to an HTTP controller. / Зависимости HTTP-контроллера.

## HttpControllerBase

Base class for HTTP controllers registered with an [Application][1].
Базовый класс HTTP-контроллеров, регистрируемых в [Application][1].
Subclasses must declare their own static `prefix` and `routes` fields, may declare static
`middleware`, and must be registered through [Application#registerHttpController][2]. A fresh
instance is created only when the middleware chain reaches the handler.
Подклассы должны объявить собственные статические поля `prefix` и `routes`, могут объявить
статическое поле `middleware` и регистрируются через [Application#registerHttpController][2].
Новый экземпляр создаётся, только когда цепочка middleware достигает обработчика.

### Parameters

- `options` **([HttpControllerOptions][3] | [undefined][4])** Controller dependencies. / Зависимости HTTP-контроллера. (optional, default `undefined`)

### jobRunner

Application-owned job runner. / Принадлежащий приложению исполнитель задач.

Type: JobRunnerCapability

### websocket

Application-wide WebSocket sender. / Общий sender WebSocket-приложения.

Type: WebSocketSenderCapability

### events

Application-wide event sender. / Общий sender внутренних событий.

Type: EventSenderCapability

[1]: ./Application.md#application
[2]: ./Application.md#registerhttpcontroller
[3]: #httpcontrolleroptions
[4]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/undefined

## TypeScript declarations / TypeScript-декларации

### `HttpControllerOptions`

```ts
export interface HttpControllerOptions {
  readonly jobRunner: JobRunnerCapability;
  readonly websocket: WebSocketSenderCapability;
  readonly events: EventSenderCapability;
}
```
