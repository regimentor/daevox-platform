<!-- Generated by documentation.js. Update this documentation by updating the source code. -->

# ScheduledTaskBase

Source / Исходный модуль: [`src/ScheduledTaskBase.ts`](../../src/ScheduledTaskBase.ts)

## ScheduledTaskOptions

Dependencies of a scheduled task. / Зависимости запланированной задачи.

Type: HttpControllerOptions

## ScheduledTaskContext

Cooperative execution context. / Контекст кооперативного выполнения.

### signal

Cancellation at application close. / Отмена при закрытии приложения.

Type: AbortSignal

## ScheduledTaskClass

Registered task constructor. / Конструктор регистрируемой задачи.

Type: {: any, cron: [string][1]}

### Properties

- `` **any**&#x20;
- `cron` **[string][1]**&#x20;

## ScheduledTaskBase

Fresh instance for each scheduled execution. / Новый экземпляр для каждого запуска.

### Parameters

- `options` **[ScheduledTaskOptions][2]**&#x20;

### jobRunner

Worker execution capability. / Возможность выполнения в Worker.

Type: any

### events

Addressed event delivery. / Адресная доставка событий.

Type: any

### websocket

WebSocket delivery capability. / Возможность доставки WebSocket.

Type: any

## ScheduledTaskErrorContext

Execution failure identity. / Идентификация ошибки выполнения.

### taskClass

Registered constructor. / Зарегистрированный конструктор.

Type: [ScheduledTaskClass][3]\<any>

### taskName

Diagnostic class name. / Диагностическое имя класса.

Type: [string][1]

### phase

Failure stage. / Этап ошибки.

Type: (`"constructor"` | `"run"` | `"shutdown"`)

## ScheduledTasksOptions

Schedule lifecycle configuration. / Конфигурация жизненного цикла расписаний.

### shutdownTimeout

Shared grace period in milliseconds (default 30000). / Общий grace-период в миллисекундах (по умолчанию 30000).

Type: [number][4]

### onError

Failure observer. / Наблюдатель ошибок.

Type: function (error: any, context: [ScheduledTaskErrorContext][5]): any

## ScheduledTaskShutdownTimeoutError

**Extends Error**

Scheduled execution exceeded its shutdown grace period. / Выполнение превысило grace-период завершения.

[1]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/String
[2]: #scheduledtaskoptions
[3]: #scheduledtaskclass
[4]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Number
[5]: #scheduledtaskerrorcontext

## TypeScript declarations / TypeScript-декларации

### `ScheduledTaskOptions`

```ts
export type ScheduledTaskOptions = HttpControllerOptions;
```

### `ScheduledTaskContext`

```ts
export interface ScheduledTaskContext {
  /** Cancellation at application close. / Отмена при закрытии приложения. @public */
  readonly signal: AbortSignal;
}
```

### `ScheduledTaskClass`

```ts
export type ScheduledTaskClass<TAppState extends object = AppStateInstance> = {
  new (options: ScheduledTaskOptions): ScheduledTaskBase & {
    run: (appState: TAppState, context: ScheduledTaskContext) => Promise<void>;
  };
  readonly cron: string;
};
```

### `ScheduledTaskErrorContext`

```ts
export interface ScheduledTaskErrorContext {
  /** Registered constructor. / Зарегистрированный конструктор. @public */
  readonly taskClass: ScheduledTaskClass<any>;
  /** Diagnostic class name. / Диагностическое имя класса. @public */
  readonly taskName: string;
  /** Failure stage. / Этап ошибки. @public */
  readonly phase: 'constructor' | 'run' | 'shutdown';
}
```

### `ScheduledTasksOptions`

```ts
export interface ScheduledTasksOptions {
  /** Shared grace period in milliseconds (default 30000). / Общий grace-период в миллисекундах (по умолчанию 30000). @public */
  shutdownTimeout?: number;
  /** Failure observer. / Наблюдатель ошибок. @public */
  onError?: (error: unknown, context: ScheduledTaskErrorContext) => unknown;
}
```
