<!-- Generated by documentation.js. Update this documentation by updating the source code. -->

# HttpRouteJsonBodyContract

Source / Исходный модуль: [`src/HttpRouteJsonBodyContract.ts`](../../src/HttpRouteJsonBodyContract.ts)

## HttpRouteJsonBodyClass

Constructable application class used as an HTTP-route JSON body contract. / Конструируемый прикладной класс контракта JSON-тела HTTP-маршрута.

Type: {: Body, schema: [HttpRouteJsonBodySchema][1]\<Body>, validators: any?}

### Properties

- `` **any**&#x20;
- `schema` **[HttpRouteJsonBodySchema][1]\<Body>**&#x20;
- `validators` **any?**&#x20;

## bodyClass

Creates a deferred descriptor for self and forward class references.
Создаёт отложенный descriptor для self- и forward-ссылок на классы.

### Parameters

- `resolver` **function (): [HttpRouteJsonBodyClass][2]\<Body>** Pure class resolver. / Чистый resolver класса.

Returns **HttpRouteJsonBodyClassReference\<Body>** Opaque class descriptor. / Непрозрачный descriptor класса.

## HttpRouteJsonBodyDescriptor

Descriptor corresponding to one supported TypeScript data field. / Descriptor одного поддерживаемого TypeScript data field.

Type: any

## HttpRouteJsonBodyField

One schema field declaration. / Одно объявление поля schema.

Type: any

## HttpRouteJsonBodySchema

Exhaustive schema corresponding to an application body class. / Исчерпывающая schema прикладного класса тела.

Type: any

## HttpRouteJsonBodyValidatorFailure

Failure returned by an application validator. / Нарушение, возвращаемое прикладным validator.

## HttpRouteJsonBodyRootValidatorFailure

**Extends HttpRouteJsonBodyValidatorFailure**

Failure returned by a class-level validator. / Нарушение, возвращаемое class-level validator.

## HttpRouteJsonBodyInput

Deep readonly JSON projection of application data fields. / Глубокая readonly JSON-проекция прикладных data fields.

Type: any

## HttpRouteJsonBodyRootValidator

Synchronous validator of a complete class subtree. / Синхронный validator полного subtree класса.

Type: function (body: [HttpRouteJsonBodyInput][3]\<Body>): ([HttpRouteJsonBodyRootValidatorFailure][4] | any | [undefined][5])

## HttpRouteJsonBodyValidationContext

Frozen location passed to a field validator. / Замороженное положение, передаваемое field validator.

## HttpRouteJsonBodyFieldValidator

Synchronous validator of one present non-null field. / Синхронный validator одного присутствующего non-null поля.

Type: function (value: Value, context: [HttpRouteJsonBodyValidationContext][6]): ([HttpRouteJsonBodyValidatorFailure][7] | [undefined][5])

## required

Requires a field to be present. / Требует присутствия поля.

Returns **[HttpRouteJsonBodyFieldValidator][8]\<Value>**&#x20;

## minLength

Requires a string or array to have at least the given length. / Требует минимальную длину строки или массива.

### Parameters

- `bound` **[number][9]**&#x20;

Returns **[HttpRouteJsonBodyFieldValidator][8]\<Value>**&#x20;

## maxLength

Requires a string or array to have at most the given length. / Требует максимальную длину строки или массива.

### Parameters

- `bound` **[number][9]**&#x20;

Returns **[HttpRouteJsonBodyFieldValidator][8]\<Value>**&#x20;

## min

Requires a number to be at least the given bound. / Требует число не меньше заданной границы.

### Parameters

- `bound` **[number][9]**&#x20;

Returns **[HttpRouteJsonBodyFieldValidator][8]<[number][9]>**&#x20;

## max

Requires a number to be at most the given bound. / Требует число не больше заданной границы.

### Parameters

- `bound` **[number][9]**&#x20;

Returns **[HttpRouteJsonBodyFieldValidator][8]<[number][9]>**&#x20;

## integer

Requires a number to be an integer. / Требует целое число.

Returns **[HttpRouteJsonBodyFieldValidator][8]<[number][9]>**&#x20;

[1]: #httproutejsonbodyschema
[2]: #httproutejsonbodyclass
[3]: #httproutejsonbodyinput
[4]: #httproutejsonbodyrootvalidatorfailure
[5]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/undefined
[6]: #httproutejsonbodyvalidationcontext
[7]: #httproutejsonbodyvalidatorfailure
[8]: #httproutejsonbodyfieldvalidator
[9]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Number

## TypeScript declarations / TypeScript-декларации

### `HttpRouteJsonBodyClass`

```ts
export type HttpRouteJsonBodyClass<Body extends object = object> = {
  new (): Body;
  readonly schema: HttpRouteJsonBodySchema<Body>;
  readonly validators?: readonly HttpRouteJsonBodyRootValidator<Body>[];
};
```

### `HttpRouteJsonBodyDescriptor`

```ts
export type HttpRouteJsonBodyDescriptor<Value> = [Value] extends [null]
  ? null
  : IsExact<Present<Value>, string> extends true
    ? StringConstructor
    : IsExact<Present<Value>, number> extends true
      ? NumberConstructor
      : IsExact<Present<Value>, boolean> extends true
        ? BooleanConstructor
        : Present<Value> extends readonly (infer Element)[]
          ? number extends Present<Value>['length']
            ? readonly [HttpRouteJsonBodyDescriptor<Element>]
            : never
          : Present<Value> extends object
            ? | HttpRouteJsonBodyClass<Present<Value>>
              | HttpRouteJsonBodyClassReference<Present<Value>>
            : never;
```

### `HttpRouteJsonBodyField`

```ts
export type HttpRouteJsonBodyField<Value> = {
  readonly type: HttpRouteJsonBodyDescriptor<Value>;
  readonly validators?: readonly HttpRouteJsonBodyFieldValidator<Present<Value>>[];
} & FieldNullability<Value>;
```

### `HttpRouteJsonBodySchema`

```ts
export type HttpRouteJsonBodySchema<Body extends object> = {
  readonly [Key in HttpRouteJsonBodyDataKey<Body>]-?: HttpRouteJsonBodyField<Body[Key]>;
};
```

### `HttpRouteJsonBodyValidatorFailure`

```ts
export interface HttpRouteJsonBodyValidatorFailure {
  readonly code: string;
  readonly message: string;
}
```

### `HttpRouteJsonBodyRootValidatorFailure`

```ts
export interface HttpRouteJsonBodyRootValidatorFailure extends HttpRouteJsonBodyValidatorFailure {
  readonly path?: readonly (string | number)[];
}
```

### `HttpRouteJsonBodyInput`

```ts
export type HttpRouteJsonBodyInput<Value> = Value extends string | number | boolean | null
  ? Value
  : Value extends readonly (infer Element)[]
    ? readonly HttpRouteJsonBodyInput<Element>[]
    : Value extends object
      ? {
          readonly [
            Key in keyof Value as Value[Key] extends (...args: any[]) => unknown ? never : Key
          ]: HttpRouteJsonBodyInput<Exclude<Value[Key], undefined>>;
        }
      : never;
```

### `HttpRouteJsonBodyRootValidator`

```ts
export type HttpRouteJsonBodyRootValidator<Body extends object> = (
  body: HttpRouteJsonBodyInput<Body>,
) =>
  | HttpRouteJsonBodyRootValidatorFailure
  | readonly HttpRouteJsonBodyRootValidatorFailure[]
  | undefined;
```

### `HttpRouteJsonBodyValidationContext`

```ts
export interface HttpRouteJsonBodyValidationContext {
  readonly path: readonly (string | number)[];
}
```

### `HttpRouteJsonBodyFieldValidator`

```ts
export type HttpRouteJsonBodyFieldValidator<Value> = (
  value: Value,
  context: HttpRouteJsonBodyValidationContext,
) => HttpRouteJsonBodyValidatorFailure | undefined;
```
