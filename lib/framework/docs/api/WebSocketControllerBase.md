<!-- Generated by documentation.js. Update this documentation by updating the source code. -->

# WebSocketControllerBase

Source / Исходный модуль: [`src/WebSocketControllerBase.ts`](../../src/WebSocketControllerBase.ts)

## WebSocketControllerOptions

Dependencies supplied to a WebSocket controller. / Зависимости WebSocket-контроллера.

## WebSocketControllerBase

Base class for named WebSocket protocol controllers.
Базовый класс именованных контроллеров WebSocket-протокола.
Subclasses declare their own static `name` and `events` fields and may declare static
`middleware`. A fresh instance is created when a resolved message middleware chain reaches the
handler.
Подклассы объявляют собственные статические поля `name` и `events` и могут объявить статическое
поле `middleware`. Новый экземпляр создаётся, когда цепочка middleware найденного сообщения
достигает обработчика.

### Parameters

- `options` **([WebSocketControllerOptions][1] | [undefined][2])** Controller dependencies. / Зависимости контроллера. (optional, default `undefined`)

### jobRunner

Application-owned job runner. / Принадлежащий приложению исполнитель задач.

Type: JobRunnerCapability

### events

Application-wide event sender. / Общий sender внутренних событий.

Type: EventSenderCapability

[1]: #websocketcontrolleroptions
[2]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/undefined

## TypeScript declarations / TypeScript-декларации

### `WebSocketControllerOptions`

```ts
export interface WebSocketControllerOptions {
  readonly jobRunner: JobRunnerCapability;
  readonly events: EventSenderCapability;
}
```
