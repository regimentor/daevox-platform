/**
 * Minimal application state shared by the framework examples.
 * Минимальное состояние приложения, общее для примеров фреймворка.
 *
 * @public
 */
// oxlint-disable-next-line typescript/no-extraneous-class -- demonstrates an AppState without hooks
export class ExampleAppState {}
